// BRUTE FORCE APPROACH
public String twoSumExists(int[] arr, int target) {
        int n = arr.length;
        for (int i = 0; i < n; i++) {
            for (int j = i + 1; j < n; j++) {
                if (arr[i] + arr[j] == target) {
                    return "YES";
                }
            }
        }
        return "NO";
  }
TC = O(n^2)
SC = O(1)


//BETTER APPROACH
  public String twoSumExists(int[] arr, int target) {
        HashMap<Integer, Integer> map = new HashMap<>();
        for (int i = 0; i < arr.length; i++) {
            int complement = target - arr[i];
            if (map.containsKey(complement)) {
                return "YES"; 
            }
            map.put(arr[i], i);
        }
        return "NO";
  }
TC = O(n)
SC = O(n)

// OPTIMISED APPROACH
if array is sorted, then we can use two-pointer approach
