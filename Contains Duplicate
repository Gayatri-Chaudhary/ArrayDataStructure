class Solution {
    public boolean containsDuplicate(int[] nums) {
        HashSet<Integer> set = new HashSet<>();
        for(int i=0;i<nums.length;i++){
            if(set.contains(nums[i])){
                return true;
            }
               set.add(nums[i]);
            
        }
        return false;
    }
}
TC = O(n)
SC = O(n)
Worst case: saare elements unique → set mein


Agar ismein array sorted hota -> then we can
for(int i = 1; i < nums.length; i++){
    if(nums[i] == nums[i-1]){
        return true;
    }
}
return false;
TC = O(N)
SC = O(1)
